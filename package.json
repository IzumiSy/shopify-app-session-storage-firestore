{
  "name": "shopify-app-session-storage-firestore",
  "version": "1.0.0",
  "description": "Firestore storage adapter for Shopify App session",
  "main": "./build/firestore.js",
  "scripts": {
    "build": "tsc",
    "test": "FIRESTORE_EMULATOR_HOST='localhost:8080' && npm run build && firebase emulators:exec 'npx ava --verbose' --only firestore --project firestore-integration-test",
    "clean": "rm -rf build"
  },
  "author": "IzumiSy",
  "license": "MIT",
  "devDependencies": {
    "@shopify/shopify-app-session-storage": "^1.1.1",
    "@shopify/shopify-app-session-storage-test-utils": "https://gitpkg.now.sh/Shopify/shopify-app-js/packages/shopify-app-session-storage-test-utils?main",
    "@tsconfig/node18": "^1.0.1",
    "ava": "^5.2.0",
    "firebase-tools": "^11.25.1",
    "install": "^0.13.0",
    "npm": "^9.6.2",
    "prettier": "^2.8.6",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@shopify/shopify-api": "^6.2.0",
    "firebase": "^9.17.1"
  }
}
